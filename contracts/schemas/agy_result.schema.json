{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agy.cloud/contracts/schemas/agy_result.schema.json",
  "title": "agy --output-format json result (P0-verified shape)",
  "description": "Fields documented by P0 experiments (docs/P0-INVENTORY.md, DESIGN §1.1). 'status' success value is 'SUCCESS'; other statuses were never observed and the consumer must treat any non-SUCCESS (or unparseable) result as failure. Field name of structured outputs (--json-schema) is NOT verified; structured output is deferred to P2 and not modeled here.",
  "type": "object",
  "required": ["conversation_id", "status", "response", "duration_seconds", "num_turns", "usage"],
  "properties": {
    "conversation_id": {"type": "string", "minLength": 1},
    "status": {"type": "string", "description": "'SUCCESS' observed in P0; consumers must classify anything else as failure."},
    "response": {"type": "string"},
    "duration_seconds": {"type": "number", "minimum": 0},
    "num_turns": {"type": "integer", "minimum": 1},
    "usage": {
      "type": "object",
      "required": ["input_tokens", "output_tokens", "thinking_tokens", "cache_read_tokens", "total_tokens"],
      "properties": {
        "input_tokens": {"type": "integer", "minimum": 0},
        "output_tokens": {"type": "integer", "minimum": 0},
        "thinking_tokens": {"type": "integer", "minimum": 0},
        "cache_read_tokens": {"type": "integer", "minimum": 0},
        "total_tokens": {"type": "integer", "minimum": 0}
      }
    }
  }
}
